/// @description Insert description here
// You can write your code in this editor
/* Draw the shadow (so the shadow is there, when I comment the draw_self(); out, I can see it 
moving, unfortunately the person whose shadows I took has a much smaller sprite that doesn't 
cover it up. */
draw_sprite(sprSarahShadow, image_index, x,y);
draw_self();
